<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/auth.service/src/main/java/auth/auth/service/service/AuthService.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/auth.service/src/main/java/auth/auth/service/service/AuthService.java" />
              <option name="originalContent" value="package auth.auth.service.service;&#10;&#10;import auth.auth.service.domain.dto.AuthResponse;&#10;import auth.auth.service.domain.dto.LoginRequest;&#10;import auth.auth.service.domain.dto.RegisterRequest;&#10;import auth.auth.service.domain.entity.User;&#10;import auth.auth.service.domain.enums.Role;&#10;import auth.auth.service.exception.BusinessException;&#10;import auth.auth.service.repository.UserRepository;&#10;import lombok.RequiredArgsConstructor;&#10;import org.springframework.security.authentication.AuthenticationManager;&#10;import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;&#10;import org.springframework.security.crypto.password.PasswordEncoder;&#10;import org.springframework.stereotype.Service;&#10;&#10;@Service&#10;@RequiredArgsConstructor&#10;public class AuthService {&#10;&#10;    private final UserRepository userRepository;&#10;    private final PasswordEncoder passwordEncoder;&#10;    private final JwtService jwtService;&#10;    private final AuthenticationManager authenticationManager;&#10;&#10;    public AuthResponse register(RegisterRequest request) {&#10;        if (userRepository.existsByEmail(request.email())) {&#10;            throw new BusinessException(&quot;Email já cadastrado&quot;);&#10;        }&#10;&#10;        var user = User.builder()&#10;                .name(request.name())&#10;                .email(request.email())&#10;                .password(passwordEncoder.encode(request.password()))&#10;                .role(Role.USER)&#10;                .active(true)&#10;                .build();&#10;&#10;        userRepository.save(user);&#10;&#10;        var accessToken = jwtService.generateToken(user);&#10;        var refreshToken = jwtService.generateRefreshToken(user);&#10;&#10;        return AuthResponse.of(accessToken, refreshToken, jwtService.getExpiration());&#10;    }&#10;&#10;    public AuthResponse login(LoginRequest request) {&#10;        authenticationManager.authenticate(&#10;                new UsernamePasswordAuthenticationToken(request.email(), request.password())&#10;        );&#10;&#10;        var user = userRepository.findByEmail(request.email())&#10;                .orElseThrow(() -&gt; new BusinessException(&quot;Usuário não encontrado&quot;));&#10;&#10;        var accessToken = jwtService.generateToken(user);&#10;        var refreshToken = jwtService.generateRefreshToken(user);&#10;&#10;        return AuthResponse.of(accessToken, refreshToken, jwtService.getExpiration());&#10;    }&#10;}" />
              <option name="updatedContent" value="package auth.auth.service.service;&#10;&#10;import auth.auth.service.domain.dto.AuthResponse;&#10;import auth.auth.service.domain.dto.LoginRequest;&#10;import auth.auth.service.domain.dto.RegisterRequest;&#10;import auth.auth.service.domain.entity.User;&#10;import auth.auth.service.domain.enums.Role;&#10;import auth.auth.service.exception.BusinessException;&#10;import auth.auth.service.repository.UserRepository;&#10;import lombok.RequiredArgsConstructor;&#10;import org.springframework.security.authentication.AuthenticationManager;&#10;import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;&#10;import org.springframework.security.crypto.password.PasswordEncoder;&#10;import org.springframework.stereotype.Service;&#10;&#10;@Service&#10;@RequiredArgsConstructor&#10;public class AuthService {&#10;&#10;    private final UserRepository userRepository;&#10;    private final PasswordEncoder passwordEncoder;&#10;    private final TokenService tokenService;&#10;    private final AuthenticationManager authenticationManager;&#10;&#10;    public AuthResponse register(RegisterRequest request) {&#10;        if (userRepository.existsByEmail(request.email())) {&#10;            throw new BusinessException(&quot;Email já cadastrado&quot;);&#10;        }&#10;&#10;        var user = User.builder()&#10;                .name(request.name())&#10;                .email(request.email())&#10;                .password(passwordEncoder.encode(request.password()))&#10;                .role(Role.USER)&#10;                .active(true)&#10;                .build();&#10;&#10;        userRepository.save(user);&#10;&#10;        var accessToken = jwtService.generateToken(user);&#10;        var refreshToken = jwtService.generateRefreshToken(user);&#10;&#10;        return AuthResponse.of(accessToken, refreshToken, jwtService.getExpiration());&#10;    }&#10;&#10;    public AuthResponse login(LoginRequest request) {&#10;        authenticationManager.authenticate(&#10;                new UsernamePasswordAuthenticationToken(request.email(), request.password())&#10;        );&#10;&#10;        var user = userRepository.findByEmail(request.email())&#10;                .orElseThrow(() -&gt; new BusinessException(&quot;Usuário não encontrado&quot;));&#10;&#10;        var accessToken = jwtService.generateToken(user);&#10;        var refreshToken = jwtService.generateRefreshToken(user);&#10;&#10;        return AuthResponse.of(accessToken, refreshToken, jwtService.getExpiration());&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>